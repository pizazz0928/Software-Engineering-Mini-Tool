# 软件工程课程实验报告

## 一、实验基本信息

- 课程名称：软件工程
- 实验名称：系统变量类型判定工具的设计与实现
- 学生姓名：
- 学号：
- 班级：
- 指导教师：
- 实验日期：

## 二、实验题目

设计并实现一个带有前后端界面的 Web 小工具，用于对给定系统变量的取值进行分析，并判断其可能对应的数据类型。

## 三、实验目的

本次实验的目的，一方面是围绕一个相对具体的功能需求，完成从需求理解、方案设计到编码实现与测试验证的完整过程；另一方面，也希望借此进一步熟悉小型 Web 系统的组织方式，以及前后端协同实现的基本方法。

结合课程内容，我主要希望通过这次实验达到以下几个目标：

1. 练习将自然语言形式的需求描述转化为可实现的软件功能。
2. 理解一个小型软件系统在结构划分、接口设计和异常处理上的基本考虑。
3. 熟悉 Flask 在轻量级后端开发中的使用方式。
4. 通过对用户输入进行解析和类型推断，加深对数据表示、边界情况与安全性的理解。

## 四、实验背景与需求分析

本实验的原始需求表述为：

> A tool for determining the type of a given system variable.

如果只按字面理解，这句话表达的是“实现一个工具，用于判断某个给定系统变量的类型”。但在具体的软件工程语境下，单纯停留在翻译层面并不足以支撑实现，还需要进一步明确这里的“system variable”究竟应如何理解。

结合实验场景，我将其理解为一种更贴近实际开发的需求：用户输入一个系统相关变量的值，例如环境变量、配置项或者某种程序参数，而系统需要根据该值的内容，判断它在运行时更适合被解释为什么类型。换句话说，这个工具并不是简单返回“输入是字符串”，而是尝试从字符串形式中恢复其更接近真实语义的数据类型，例如整数、浮点数、布尔值、列表、字典等。

这一理解与实际开发经验是吻合的。很多配置项在读取时本质上都是文本，但在程序中通常并不会以纯字符串的方式使用。例如，`5001` 常常意味着端口号，`true` 对应布尔开关，`{"debug": false}` 则更接近结构化配置。因此，一个“类型判定工具”的价值就在于帮助开发者快速识别这些值可能的实际含义。

基于上述理解，本实验的需求可以分解为三个基本部分：

### 1. 输入

系统接收用户输入的变量值。输入形式统一为字符串，但内容可能属于不同的数据表示方式。

### 2. 处理

系统对输入内容进行逐步分析，包括数值转换、布尔值识别、结构化数据解析以及必要的表达式判断。

### 3. 输出

系统输出原始输入、推断得到的数据类型、解析后的值，以及在有必要时补充说明其解析方式。

进一步整理后，可以得到以下功能性要求：

1. 提供图形化页面，便于输入与查看结果。
2. 提供后端接口，负责接收请求并完成类型分析。
3. 支持识别常见基本类型和部分复合类型。
4. 对无法归入其他类型的输入，能够回退为字符串。
5. 对异常输入和接口错误进行基本处理，保证系统可用性。

除功能需求外，还需要兼顾以下几点：

1. 界面应尽量简洁，便于演示和使用。
2. 类型判断逻辑需要尽量安全，避免直接执行不可信输入。
3. 项目结构应保持清晰，便于后续扩展和课程展示。

## 五、系统总体设计

本系统采用 Flask 作为后端框架，整体结构比较轻量，主要分为前端展示层和后端处理层两部分。考虑到本实验的重点在于“功能闭环”而非复杂架构，因此没有刻意引入额外的工程化组件，而是尽量保持实现简洁、职责清晰。

### 1. 架构说明

- 前端部分使用 HTML、CSS 和 JavaScript 实现页面展示与交互逻辑。
- 后端部分使用 Python 与 Flask 实现页面路由、接口路由及核心类型推断逻辑。
- 前后端之间通过 JSON 进行数据交互。

用户在浏览器中输入变量值后，前端将其发送给后端接口；后端完成分析后，再将结果返回给前端进行展示。整个流程较短，适合作为课程实验中的完整样例。

### 2. 模块划分

从实现角度看，系统可以划分为以下几个模块：

1. 页面展示模块：负责输入区域、结果区域和基本交互元素的组织。
2. 前端交互模块：负责提交请求、接收结果并更新界面。
3. 接口服务模块：负责请求校验、结果封装和异常处理。
4. 类型推断模块：负责根据输入内容执行具体的类型识别逻辑。

这样的划分虽然简单，但已经能够体现出前端、接口和业务逻辑之间相对清晰的边界。

## 六、详细设计

### 1. 前端设计

前端页面采用单页形式组织，主要包括标题说明区、输入区、按钮区和结果展示区。考虑到这是一个课程小工具，我没有在界面上追求过于复杂的布局，而是尽量让用户在进入页面后能够直接理解用途，并快速完成一次输入和分析。

其中，输入区使用 `textarea` 以便用户输入较长内容，例如 JSON 或列表；按钮区提供“分析”和“示例填充”两个操作；结果区则分别展示推断类型、解析后的值、原始输入以及附加说明。这样的设计有助于将“分析过程”和“分析结果”分离开来，提升可读性。

### 2. 后端接口设计

后端提供两个路由：

1. `/`：返回前端页面。
2. `/api/analyze`：接收前端提交的变量值，并返回分析结果。

接口采用 `POST` 方法，请求体使用 JSON 格式，例如：

```json
{
  "variable_value": "8 * (2 + 1)"
}
```

返回结果中包含以下几个字段：

- `original_input`：原始输入内容
- `inferred_type`：系统推断出的类型
- `parsed_value`：解析后的值
- `warning`：补充说明信息，在需要时返回

这样的返回结构比较直接，也便于前端按字段展示。

### 3. 类型推断逻辑设计

本实验的核心在于类型推断函数的设计。由于所有输入在进入系统时都是字符串，因此需要通过一组有顺序的判断逻辑，尽可能恢复其潜在语义。

我采用的判断顺序如下：

1. 判断是否为空输入。
2. 尝试解析为整数。
3. 尝试解析为浮点数。
4. 判断是否属于布尔值关键字。
5. 尝试识别为简单算术表达式。
6. 尝试按 JSON 解析。
7. 尝试按 Python 字面量解析。
8. 若都失败，则视为普通字符串。

这种顺序并非唯一，但在本实验场景下是比较合理的。一方面，它能够较好覆盖日常配置项中常见的表示形式；另一方面，也能避免某些类型之间的歧义。例如，纯数字应该优先判断为数值类型，而不应过早落入字符串或其他解析分支。

### 4. 表达式处理与安全性考虑

在实现过程中，是否支持简单算术表达式是一个需要权衡的问题。如果完全不支持，那么像 `8 * (2 + 1)` 这样的输入就只能被当作普通字符串；如果直接使用 `eval()` 计算，又会带来明显的安全风险。

基于这一考虑，我最终选择使用 AST 白名单的方式来处理表达式。具体做法是先将输入解析为抽象语法树，然后仅允许数值常量、单目运算和常见二元算术运算参与计算。这样既能保留一定的表达式识别能力，也可以避免执行函数调用、属性访问或其他潜在危险操作。

从课程实验的角度看，这一处理方式也比较能体现“功能实现”和“安全意识”之间的平衡。

## 七、系统实现

### 1. 项目结构

本项目的主要目录结构如下：

```text
.
|-- app.py
|-- requirements.txt
|-- README.md
|-- REPORT_TEMPLATE.md
|-- static
|   |-- app.js
|   `-- styles.css
`-- templates
    `-- index.html
```

### 2. 主要文件说明

- `app.py`：后端主程序，包含 Flask 应用初始化、接口实现和核心类型推断逻辑。
- `templates/index.html`：页面结构模板。
- `static/styles.css`：页面样式文件。
- `static/app.js`：页面交互脚本，负责发起请求和更新结果。
- `requirements.txt`：项目依赖说明。
- `README.md`：项目使用说明文档。

### 3. 实现说明

从实现过程来看，系统规模不大，但包含了一个完整的小型 Web 应用所需要的基本要素：页面展示、前端交互、后端接口、核心业务逻辑和文档说明。用户在网页中输入内容后，前端通过异步请求将数据发送到后端，后端完成分析后返回结构化结果，前端再将其展示出来。

虽然整体实现较为直接，但我认为这种简洁结构比较符合本实验的目标，也便于后续继续扩展，例如增加更多类型识别规则、加入测试模块，或者引入配置管理机制等。

## 八、测试设计与结果分析

为了验证系统功能是否符合预期，我选取了若干具有代表性的输入进行测试，包括基本数值、布尔值、JSON、列表、算术表达式以及普通字符串。测试结果如下表所示：

| 输入内容 | 预期类型 | 实际结果 | 是否通过 |
|---|---|---|---|
| `42` | Integer | Integer | 是 |
| `3.14` | Float | Float | 是 |
| `true` | Boolean | Boolean | 是 |
| `{"name":"demo","debug":false}` | Dict | Dict | 是 |
| `[1,2,3]` | List | List | 是 |
| `8 * (2 + 1)` | Integer | Integer | 是 |
| `hello world` | String | String | 是 |

从测试结果可以看出，系统对常见输入类型的识别是符合预期的。尤其是在 JSON 和算术表达式这两类稍复杂的输入上，系统能够给出较为合理的分析结果，并通过附加说明提示用户其解析方式。

需要说明的是，本系统并不追求覆盖所有可能的数据表示形式，而是面向课程实验场景，重点支持较常见、较典型的输入。如果后续需要面向更复杂的实际场景使用，仍然需要在规则覆盖、优先级设计和错误处理上进一步完善。

## 九、实验中遇到的问题及处理过程

### 1. 如何理解“系统变量类型判定”这一需求

实验开始时，最大的困难并不在编码本身，而在于对需求的把握。原始描述非常简短，如果仅仅理解为“对变量调用 `type()`”，那实现会过于表面，也无法体现这个工具的实际价值。

因此，我在实现之前首先对需求做了重新解释，将重点放在“字符串形式的系统变量值如何恢复其潜在类型语义”上。这个过程让我更加意识到，软件工程中的需求分析往往并不是简单翻译，而是要结合上下文补足实现所需的信息。

### 2. 如何在支持表达式的同时控制风险

另一个比较关键的问题是表达式处理。如果完全不支持表达式，系统能力会有所受限；但如果直接采用 `eval()`，又会引入明显的安全隐患。

对此，我最终采用 AST 白名单的方式进行限制，只允许安全且必要的节点参与求值。虽然这增加了一定实现成本，但从结果来看是值得的。

### 3. 前后端联调中的数据一致性问题

在前后端交互过程中，需要保证字段命名、数据格式以及异常处理方式保持一致，否则页面展示容易出现混乱。因此，我在接口设计中尽量保持返回结构稳定，避免前端需要根据多种情况分别处理。

## 十、实验总结

通过本次实验，我完成了一个较完整的小型课程项目。整个过程虽然不算复杂，但基本覆盖了软件工程实践中几个比较核心的环节，包括需求理解、模块划分、接口设计、功能实现、安全性考虑以及结果验证。

与单纯编写一个函数相比，这次实验更让我体会到“系统实现”的含义。即使是一个功能相对聚焦的小工具，只要真正做成可运行、可展示、可说明的项目，就需要从整体结构而不是局部代码去考虑问题。

另外，本次实验也让我更加重视输入处理的边界和安全性。很多时候，一个功能是否“能运行”只是最低要求，而如何在合理范围内保证其稳定性和可解释性，往往更能体现设计质量。

总体而言，本实验达到了预期目标。最终实现的系统具备前后端完整结构，能够较准确地判断给定变量值的可能类型，也较好地回应了题目提出的需求。

## 十一、参考文献

1. Python 官方文档
2. Flask 官方文档
3. 软件工程课程教材
4. JSON 与 AST 相关技术资料

## 十二、附录

### 1. 运行方式

```bash
pip install -r requirements.txt
python app.py
```

浏览器访问地址：

```text
http://127.0.0.1:5001
```

### 2. 建议补充的材料

在最终提交时，可以根据老师要求补充以下内容，以增强报告的完整性：

1. 系统首页截图
2. 输入不同类型数据后的页面结果截图
3. 项目运行截图或接口测试截图
4. 关键代码片段说明
